This example allows to read and write hardware registers and memory over serial.
Related scripts:
puppettrer.py can be used to send read/write command
emulator.py emulate the original ROM using py8051 and forward all registers
read + write to the device.

baudrate: 9600
TX pin:   blue led (left of the audio port)
RX pin:   unpopulated header J2 pin 2.

!!! WARNING !!! flashing this firmware will soft brick your device.
To unbrick, short out the SPI flash to boot into ROM mode and use the included
flasher to restore the firmware.
